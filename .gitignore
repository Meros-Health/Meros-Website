# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.development
.env.staging
.env.production

# Wrangler's local secret file. Holds the same values as `wrangler secret put`
# (RESEND_API_KEY), so it is a credential file and never belongs in the repo.
# The .env* catch-all below does not match it.
.dev.vars
.dev.vars.*

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright
.playwright/
.playwright-cli/

# cursor
.cursor/

# Paid / licensed brand assets: keep local only, never commit.
# Drop files into these folders for local dev and deploy builds.
/public/fonts/*
!/public/fonts/.gitkeep
!/public/fonts/README.md
!/public/fonts/Aetheria.woff
!/public/fonts/MontageSerif-Regular.woff2
!/public/fonts/Montage Serif Font Regular.otf
!/public/fonts/Satoshi-Light.woff2
!/public/images-web/**
!/public/logos/**

# Internal brand documents: sensitive, never commit to public repo.
/documents/

# Internal store operations, SOPs, and compliance records. Never commit to public repo.
/MEROS-resources/

# Catch-all for any .env variant not covered above
.env*

# graphify: local knowledge-graph cache and build artifacts, never commit
/graphify-out/

# cloudflare / opennext build artifacts
/.open-next/
/.wrangler/

# Internal infra/DNS runbook (account details, third-party vendor relationship). Never commit to public repo.
/docs/dns-cutover.md

# Same class: a consolidated map of who sends and receives mail for the domain
# (M365 tenant, reseller, SPF/DMARC posture). Each record is public in DNS; the
# map is not, and this repo already keeps that line for the runbook above.
/docs/mail-estate.md

# test output
/test-results/
/playwright-report/

# Committed template, no secrets
!.env.example
/public/img/
