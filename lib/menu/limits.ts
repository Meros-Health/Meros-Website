// Order limits shared by the cart store, the drawer controls and the checkout
// action, so the UI can never disagree with the server about them.
export const MAX_QUANTITY = 99;
export const MAX_LINES = 50;
