/**
 * Launch gates.
 *
 * CHECKOUT_ENABLED — the ordering system isn't live yet, so checkout is
 * intentionally unreachable: the cart drawer shows a disabled "Coming Soon"
 * button and /checkout redirects to /order. Flip to `true` to enable both.
 */
export const CHECKOUT_ENABLED = false;
