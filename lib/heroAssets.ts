// Shared between HeroSection (renders them) and Preloader (preloads them) —
// keeping one source of truth avoids the two drifting out of sync.
export const HERO_BG_SRC = "/images-web/Hero/Gallery-4-hero.jpg";
// Combined lockup — "MEROS" + "HOUSE OF YOGURT" baked into one image, so the
// tagline never needs its own separate DOM text.
export const HERO_LOGO_SRC = "/logos/name-light.png";
