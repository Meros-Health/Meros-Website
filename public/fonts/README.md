# Fonts (local only — not committed)

Paid/licensed font files live here for local dev and deploy builds.
They are gitignored and must never be committed.

Required for the site:

- `Montage Serif Font Regular.otf` — headlines / wordmark
- `Aetheria.woff` — accent / editorial text

Optional / unused formats can stay on disk but are not loaded by the app.
